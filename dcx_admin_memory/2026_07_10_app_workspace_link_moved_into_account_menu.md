# App workspace link moved into account menu

The `App workspace` cross-surface link moved from the standalone admin sidebar footer into the account pop-out immediately above `Log out`.

The admin access-blocked copy now names only the exact supported roles: `admin`, `dev`, and `shareholder`.

The production build passes. Repository-wide lint still reports established unrelated errors; the changed menu components pass targeted lint.
